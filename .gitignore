# Per-character data files (generated by the inventory module)
data/

# Per-machine extracted item icons (cached BMPs pulled from FFXI DAT
# files at runtime — regenerated on first use, don't commit them).
cache/

# Editor / OS junk
*.swp
*.swo
*~
*.bak
Thumbs.db
Desktop.ini
.DS_Store
